# Copy this file to .env.local so the admin app can reach the API gateway.
# Base URL for the API Gateway (no trailing slash)
# e.g. http://localhost:3000/api or https://your-api.render.com/api
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api
